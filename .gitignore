# OS / editor cruft
.DS_Store
**/.DS_Store
Thumbs.db

# Local tooling / settings
.claude/
*.local.json

# Secrets & local-only config (never commit)
lukasz/mission-config.local.js
vfxtools/bookmark-app/config.php

# Personal / runtime data (PII)
frameshift/subscribers.json
vfxtools/bookmark-app/articles.json
vfxtools/bookmark-app/articles_old.json
vfxtools/bookmark-app/articles_backup_*.json

# Archived dashboard (contained old secrets)
lukasz/archive/

# Dependencies / build
node_modules/

# Babcia Chat (Day 04) — backend secrets/deps (never commit)
babcia-server/.env
babcia-server/node_modules/

# Deeplight (Day 02) — local Python authoring env (never commit/deploy)
lab/deeplight/tools/.venv/
**/__pycache__/
# authoring inputs + debug overlays (not needed live; only level 1 ships)
lab/deeplight/levels/source/
lab/deeplight/levels/*.debug.png
